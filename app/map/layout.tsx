import type { ReactNode } from "react";

/** Карта на всю высоту экрана: без футера, main растягивается. */
export default function MapLayout({ children }: { children: ReactNode }) {
  return children;
}
